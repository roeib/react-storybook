import React from 'react';

/**
 * Primary UI component for user interaction
 */
export const Roei = ({label, ...args}) => {
  return (
    <div
    {...args}>
      this is a dummy div {label}
    </div>
  );
};

